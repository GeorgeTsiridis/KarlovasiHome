<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:KarlovasiHome.ViewModels;assembly=KarlovasiHome"
             xmlns:circleImage="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             x:Class="KarlovasiHome.Views.MenuPage"
             Title="Menu">

    <ContentPage.BindingContext>
        <viewModels:MenuViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Grid HeightRequest="300" BackgroundColor="#f27449">
            <circleImage:CircleImage HorizontalOptions="Center" VerticalOptions="Center" HeightRequest="150" WidthRequest="150" Aspect="AspectFill" BorderThickness="2" BorderColor="Black" Source="http://upload.wikimedia.org/wikipedia/commons/5/55/Tamarin_portrait.JPG">
                <circleImage:CircleImage.GestureRecognizers>
                    <TapGestureRecognizer Tapped="ProfileImage_OnTapped"/>
                </circleImage:CircleImage.GestureRecognizers>
            </circleImage:CircleImage>
            <StackLayout BackgroundColor="Black" HeightRequest="2" VerticalOptions="End"/>
        </Grid>
        <ListView x:Name="ListViewMenu" HasUnevenRows="True" ItemsSource="{Binding MenuItems}" ItemTapped="ListViewMenu_OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Title}" FontSize="25" HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>